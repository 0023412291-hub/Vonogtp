/**
 * Bản thay thế cho WEB — không dùng react-native-maps (native-only).
 * Màn tìm kiếm tự rơi vào thông báo "bản đồ chỉ có trên di động" khi MapView = null.
 */
export const MapView = null;
export const Marker = null;
